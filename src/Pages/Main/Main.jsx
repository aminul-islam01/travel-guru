import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Main = () => {
    const travels = useLoaderData();
    return (
        <div>
            
        </div>
    );
};

export default Main;